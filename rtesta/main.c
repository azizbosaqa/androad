#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a,b ;
    int c=1,k;
    long res,tot ;
    scanf("%f%f",&a,&b) ;
    while
    {
        k=c+a ;
    while (k%2!=0)
        {
        res=k*k ;
        ++k ;
        }
        tot+=res;
    }while(k<b);

    printf("%d",tot);
    return 0;
}
